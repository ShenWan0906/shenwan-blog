---
import MainGridLayout from "../layouts/MainGridLayout.astro";
import { projects } from "../constants/projects"; // 数据源
import { Icon } from "astro-icon/components";
import defaultCover from "../constants/images/cover.jpeg"; // 兜底图

const title = "项目作品集";
const description = "西瓜主管的项目经验与技术成果展示";

// 默认样式兜底 (防止万一 data 里没写 style)
const FALLBACK_STYLE = {
    gradient: "from-gray-700 via-gray-800 to-black",
    icon: "material-symbols:code-blocks-outline-rounded"
};
---

<MainGridLayout title={title} description={description}>
  
  <div class="flex w-full rounded-[var(--radius-large)] overflow-hidden relative mb-6">
    <div class="card-base z-10 px-6 md:px-9 pt-8 pb-8 relative w-full bg-gradient-to-br from-white/90 to-white/50 dark:from-black/20 dark:to-black/5 backdrop-blur-md border border-black/5 dark:border-white/5">
        <Icon name="material-symbols:rocket-launch-outline-rounded" class="absolute right-6 top-6 text-9xl opacity-5 -rotate-12 pointer-events-none text-[var(--primary)]" />
        <h1 class="text-2xl md:text-3xl font-bold text-black/90 dark:text-white/90 mb-3 flex items-center gap-3">
            <span class="text-[var(--primary)]">🎯</span> 我的项目工坊
        </h1>
        <p class="text-black/60 dark:text-white/60 text-sm md:text-base leading-relaxed max-w-2xl">
            这里陈列了我职业生涯中的关键战役。作为 <span class="text-[var(--primary)] font-bold">项目经理</span> 与 <span class="text-[var(--primary)] font-bold">全栈开发</span>，
            我不仅关注代码的优雅，更在意系统架构的稳健与业务价值的交付。
        </p>
    </div>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-5 mb-8">
    {projects.map((project) => {
      // ✨ 直接从数据中获取样式，没有复杂的 if-else 判断了
      const style = project.style || FALLBACK_STYLE;
      
      return (
      <a 
        href={project.link || "#"} 
        class="group relative flex flex-col h-full rounded-2xl transition-all duration-300 hover:-translate-y-1.5"
      >
        <div class="card-base h-full flex flex-col overflow-hidden bg-white dark:bg-[#1e1e1e] shadow-sm hover:shadow-xl dark:hover:shadow-black/50 border border-black/5 dark:border-white/5 relative z-10">
            
            <div class="relative w-full aspect-[16/9] overflow-hidden bg-gray-100 dark:bg-gray-800">
                {/* 优先显示图片 */}
                {project.image && project.image.length > 5 ? (
                    <img 
                        src={project.image} 
                        alt={project.title} 
                        onerror={`this.onerror=null; this.src='${defaultCover.src}';`}
                        class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
                    />
                ) : (
                    /* 没有图片 -> 直接渲染配置好的渐变 */
                    <div class={`w-full h-full bg-gradient-to-br ${style.gradient} relative group-hover:scale-110 transition-transform duration-700`}>
                        
                        <div class="absolute inset-0 opacity-20" 
                             style="background-image: radial-gradient(rgba(255,255,255,0.3) 1px, transparent 1px); background-size: 20px 20px;">
                        </div>

                        <div class="absolute inset-0 flex items-center justify-center">
                            <div class="bg-white/20 backdrop-blur-md border border-white/20 p-4 rounded-2xl text-white shadow-lg group-hover:scale-110 transition-transform duration-500">
                                {/* 图标也由配置决定 */}
                                <Icon name={style.icon} class="text-4xl md:text-5xl drop-shadow-md" />
                            </div>
                        </div>
                    </div>
                )}
                
                <div class="absolute inset-0 bg-black/0 group-hover:bg-black/10 transition-colors duration-300"></div>

                <div class="absolute top-3 left-3 flex items-center gap-1 bg-black/40 backdrop-blur-md text-white text-xs font-bold px-2.5 py-1 rounded-full border border-white/10 shadow-lg">
                    <Icon name="material-symbols:calendar-month-outline" class="text-xs" />
                    {project.year}
                </div>

                <div class="absolute bottom-3 right-3 translate-y-4 opacity-0 group-hover:translate-y-0 group-hover:opacity-100 transition-all duration-300">
                    <div class="bg-white text-black p-2 rounded-full shadow-lg">
                        <Icon name="material-symbols:arrow-outward-rounded" />
                    </div>
                </div>
            </div>

            <div class="p-5 flex flex-col flex-1 relative">
                <div class="mb-3">
                    <span class="inline-flex items-center gap-1.5 text-xs font-bold px-2.5 py-1 rounded-md text-blue-600 bg-blue-50 dark:text-blue-300 dark:bg-blue-900/30 border border-blue-100 dark:border-blue-500/20">
                        <Icon name="material-symbols:person-play" class="text-sm" />
                        {project.role}
                    </span>
                </div>

                <h3 class="text-lg md:text-xl font-bold text-black/90 dark:text-white/90 mb-2 line-clamp-1 group-hover:text-[var(--primary)] transition-colors">
                    {project.title}
                </h3>

                <p class="text-sm text-black/60 dark:text-white/50 line-clamp-2 mb-4 leading-relaxed">
                    {project.description}
                </p>

                <div class="mt-auto border-t border-black/5 dark:border-white/5 pt-4">
                    <div class="flex flex-wrap gap-2">
                        {project.tags.map(tag => (
                            <span class="text-[10px] md:text-xs px-2 py-1 rounded bg-gray-100 dark:bg-gray-800 text-gray-500 dark:text-gray-400 group-hover:bg-[var(--primary)]/10 group-hover:text-[var(--primary)] transition-colors duration-300">
                                #{tag}
                            </span>
                        ))}
                    </div>
                </div>

            </div>
        </div>

        <div class="absolute -inset-0.5 bg-gradient-to-r from-[var(--primary)] to-blue-600 rounded-2xl opacity-0 group-hover:opacity-20 blur transition duration-500 dark:group-hover:opacity-40"></div>
      </a>
    )})}
  </div>

  <div class="flex justify-center mb-4">
    <a href="/about" class="text-sm text-black/40 dark:text-white/40 hover:text-[var(--primary)] transition-colors flex items-center gap-1">
        想要了解更多关于我的信息？ <Icon name="material-symbols:arrow-right-alt-rounded" />
    </a>
  </div>

</MainGridLayout>