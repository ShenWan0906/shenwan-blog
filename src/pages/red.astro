---
import MainGridLayout from "../layouts/MainGridLayout.astro";
import { redPosts } from "../constants/xiaohongshu";
import { Icon } from "astro-icon/components";

const title = "小红书精选";
const description = "探索西瓜的更多生活碎片与干货分享";
---

<MainGridLayout title={title} description={description}>
  
  {/* 1. 头部介绍 */}
  <div class="flex w-full rounded-[var(--radius-large)] overflow-hidden relative mb-6">
    <div class="card-base z-10 px-6 md:px-9 pt-8 pb-8 relative w-full">
        <Icon name="material-symbols:menu-book-rounded" 
              class="absolute right-6 top-6 text-9xl -rotate-12 pointer-events-none text-[#ff2442] opacity-5 dark:opacity-[0.05]" />
        
        <h1 class="text-2xl md:text-3xl font-bold mb-3 flex items-center gap-3 text-[#ff2442] dark:text-[#ff475e]">
            <Icon name="material-symbols:local-fire-department-rounded" /> 种草与生活志
        </h1>
        <p class="text-sm md:text-base leading-relaxed max-w-2xl text-[var(--content)] dark:text-gray-300 opacity-80">
            这里有我的生活碎片、好物种草以及旅游攻略。
            <br/>点击卡片阅读 <span class="font-bold text-[#ff2442]">完整图文笔记</span>。
        </p>
    </div>
  </div>

  {/* 2. 瀑布流卡片列表 */}
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5 mb-8">
    {redPosts.map((post, index) => (
      <button 
        onclick={`document.getElementById('red-dialog-${index}').showModal()`}
        class="group relative flex flex-col h-full rounded-2xl transition-all duration-300 hover:-translate-y-1.5 text-left w-full"
      >
        <div class="card-base h-full flex flex-col overflow-hidden relative z-10 rounded-2xl transform-gpu transition-all duration-300 group-hover:shadow-xl group-hover:border-[#ff2442]/30">
            
            <div class="relative w-full aspect-[3/4] overflow-hidden bg-[var(--btn-content)]/5">
                <img 
                    src={post.cover} 
                    alt={post.title} 
                    class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105 will-change-transform dark:brightness-90 dark:group-hover:brightness-100"
                />
                <div class="absolute inset-0 bg-black/0 group-hover:bg-black/5 transition-colors duration-300"></div>
                
                <div class="absolute top-3 right-3 opacity-0 group-hover:opacity-100 transition-opacity duration-300 bg-white/80 dark:bg-black/50 backdrop-blur text-[#ff2442] p-1.5 rounded-full shadow-lg">
                    <Icon name="material-symbols:zoom-in-rounded" class="text-lg" />
                </div>
            </div>

            <div class="p-4 flex flex-col flex-1 relative bg-white dark:bg-[#1e1e1e]">
                <h3 class="text-base md:text-lg font-bold mb-2 line-clamp-2 transition-colors text-[var(--content)] dark:text-gray-100 group-hover:text-[#ff2442]">
                    {post.title}
                </h3>
                <p class="text-xs line-clamp-2 mb-3 leading-relaxed text-[var(--content)] opacity-60 dark:text-gray-400">
                    {post.description}
                </p>
                
                <div class="mt-auto pt-3 border-t border-[var(--line-divider)] flex justify-between items-center text-xs text-gray-500 dark:text-gray-400">
                    <span class="opacity-80">{post.date}</span>
                    <div class="flex items-center gap-1 opacity-80 group-hover:text-[#ff2442] transition-colors">
                        <Icon name="material-symbols:favorite-rounded" class="text-sm" />
                        <span>{post.likes}</span>
                    </div>
                </div>
            </div>
        </div>
      </button>
    ))}
  </div>

  {/* 3. 小红书风格详情弹窗 (左右分栏布局) */}
  {redPosts.map((post, index) => (
    <dialog id={`red-dialog-${index}`} class="modal p-0 rounded-3xl backdrop:bg-black/80 backdrop:backdrop-blur-sm w-[95vw] md:w-[85vw] max-w-5xl max-h-[90vh] overflow-hidden bg-white dark:bg-[#1e1e1e] shadow-2xl relative">
      
      <div class="flex flex-col md:flex-row h-full max-h-[90vh]">
        
        {/* 左侧：图片展示区 */}
        <div class="w-full md:w-[55%] h-[40vh] md:h-[90vh] relative bg-zinc-100 dark:bg-black flex items-center justify-center overflow-hidden shrink-0">
          <img src={post.cover} class="absolute inset-0 w-full h-full object-cover blur-2xl opacity-40 scale-125 pointer-events-none" alt="" />
          <img src={post.cover} class="relative z-10 w-full h-full object-contain drop-shadow-2xl" alt={post.title} />
          
          <form method="dialog" class="absolute top-4 right-4 z-50 md:hidden">
            <button class="p-2 rounded-full bg-black/40 text-white backdrop-blur-md">
              <Icon name="material-symbols:close-rounded" class="text-xl" />
            </button>
          </form>
        </div>

        {/* 右侧：图文交互区 */}
        <div class="w-full md:w-[45%] h-[50vh] md:h-[90vh] p-5 md:p-8 flex flex-col relative overflow-hidden">
          
          <form method="dialog" class="absolute top-6 right-6 z-50 hidden md:block">
             <button class="p-2 rounded-full bg-gray-100 dark:bg-zinc-800 hover:bg-gray-200 dark:hover:bg-zinc-700 text-gray-600 dark:text-gray-300 transition-colors">
               <Icon name="material-symbols:close-rounded" class="text-xl" />
             </button>
          </form>

          <div class="flex items-center gap-3 mb-6 pr-10 shrink-0">
            <div class="w-10 h-10 rounded-full bg-gradient-to-tr from-[#ff2442] to-orange-400 flex items-center justify-center text-white font-bold text-lg shadow-md">
               🍉
            </div>
            <div>
               <div class="text-sm font-bold text-[var(--content)] dark:text-white">西瓜</div>
               <div class="text-xs text-gray-500">{post.date}</div>
            </div>
          </div>

          <div class="overflow-y-auto custom-scrollbar pr-2 mb-4 flex-1">
             <h2 class="text-xl font-bold text-[var(--content)] dark:text-white mb-4 leading-snug">
                {post.title}
             </h2>
             <div class="text-sm md:text-base text-gray-700 dark:text-gray-300 space-y-4 leading-relaxed opacity-90">
                {post.content.map(paragraph => (
                   <p>{paragraph}</p>
                ))}
             </div>
          </div>

          <div class="mt-auto pt-4 border-t border-[var(--line-divider)] flex items-center justify-between shrink-0 bg-white dark:bg-[#1e1e1e]">
             <div class="flex items-center gap-4 text-gray-500 dark:text-gray-400">
                <div class="flex items-center gap-1.5 hover:text-[#ff2442] transition-colors cursor-pointer">
                   <Icon name="material-symbols:favorite-outline-rounded" class="text-2xl" />
                   <span class="text-sm font-bold">{post.likes}</span>
                </div>
                <div class="flex items-center gap-1.5 hover:text-[#ff2442] transition-colors cursor-pointer">
                   <Icon name="material-symbols:star-outline-rounded" class="text-2xl" />
                   <span class="text-sm font-bold">收藏</span>
                </div>
             </div>
             
             <a href={post.link} target="_blank" rel="noopener noreferrer" class="px-5 py-2.5 bg-[#ff2442] text-white rounded-full text-sm font-bold hover:bg-[#e01f39] transition-transform active:scale-95 flex items-center gap-1.5 shadow-lg shadow-red-500/20">
                去小红书 <Icon name="material-symbols:open-in-new-rounded" />
             </a>
          </div>

        </div>
      </div>
    </dialog>
  ))}

  <script is:inline>
    const dialogs = document.querySelectorAll('dialog');
    dialogs.forEach(dialog => {
      dialog.addEventListener('click', (event) => {
        if (event.target === dialog) {
          dialog.close();
        }
      });
    });
  </script>

  <style>
    dialog[open] {
      animation: slide-up 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    }
    @keyframes slide-up {
      from { transform: translateY(20px) scale(0.98); opacity: 0; }
      to { transform: translateY(0) scale(1); opacity: 1; }
    }
  </style>

</MainGridLayout>